toString 메소드

println 또는 print에 인스턴스의 참조 값이 전달되면 해당 참조 값이 참조하는 인스턴스의 toString 메소드가 
반환하는 문자열 출력!

public String toString() {
	return const;
}

printf 메소드

System.out.printf("정수는 %d. 실수는 %f, 문자는 %c", 12, 24.5, 'A');

C와 동일하게 출력할 수 있다.

printf 메소드 호출의 예

Scanner 클래스

String source = "1 3 5"
Scanner sc = new Scanner(source);	// Scanner 인스턴스 생성
int num1 = sc.nextInt()

Scanner sc = new Scanner(System.in);

Scanner 인스턴스 생성 이후에 데이터를 스캔하는 방법에 있어서는 차이가 없다.

String str1 = sc.nextLine();


1차원 배열의 이해와 선언 방법

int[] ref;
ref = new int[5];

int[] ref = new int[5];

int[] arr = new int[3];

int[] arr = new int[] {1, 2, 3};

int[] arr = {1, 2, 3};

int ar[] = new int[3];

int[] ar = new int[3];

int[] ar = new int[10];

String[] ar = new String[10]

java.util.Arrays
